A node boilerplate with the MongoDB as the database.
It contains the following npm packages: express, nodemon, bcrypt, express-async-errors, joi, joi-object-id, jsonwebtoken, lodash, mongoose and winston. 

To use clone the project then run 
npm install
